# ============================================================
# 前端环境变量示例
# 复制为 .env.local(本地)或配置到 Vercel / GitHub Secrets(部署)
# 说明:暴露给浏览器的变量必须以 VITE_ 开头,否则 Vite 不会注入。
# ============================================================

# REST API 基础地址(如 https://api.example.com)
# 留空 = 前端自动运行在 Mock 模式(内置模拟数据,无需后端)
VITE_API_BASE_URL=

# WebSocket 地址(如 wss://api.example.com)
# 留空 = WebSocket 自动转接内置 Mock 事件总线
VITE_WS_URL=

# ============================================================
# 重要提醒
# ------------------------------------------------------------
# TOKEN_SECRET(以及任何鉴权/签名密钥)属于「后端服务」的机密,
# 严禁以 VITE_ 前缀暴露到前端 —— 所有 VITE_ 变量都会被打包进
# 浏览器 bundle,任何用户都能在产物里读到。
#
# 后端相关密钥应配置在后端部署环境(服务器 / Serverless 环境变量),
# 前端只通过 REST 与 WebSocket 协议与后端交互,不直接持有该密钥。
# ============================================================
